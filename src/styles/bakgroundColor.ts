import {StyleProp, ViewStyle} from 'react-native';
import colors from '../constant/color';

export const overlayBackground: StyleProp<ViewStyle> = {
  backgroundColor: colors.black,
  opacity: 0.7,
};
